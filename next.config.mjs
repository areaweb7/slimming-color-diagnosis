/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/slimming-color-diagnosis',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
